#
# ~/config/gtk/gtkrc-2.0
#

# Use Clearlooks theme
include "/usr/share/themes/Clearlooks/gtk-2.0/gtkrc"

#
# gVim hook to keep background color in GTK window for overflowing pixels.
#
# This is because gVim will take the maximum place in a fixed font size
# multiple, and let the remaining pixels as is.
#
# The color defined below has to match the `NonText` `guibg` color of theme
# used theme.
#
style "gvim" {bg[NORMAL] = "#252525"}
widget "vim-main-window.*GtkForm" style "gvim"
