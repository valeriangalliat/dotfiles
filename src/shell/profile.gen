#
# ~/.config/shell/profile.gen
#
# Cached profile generator.
#

GRACEFUL_EDITOR='vim,vi'
GRACEFUL_BROWSER='firefox,w3m,elinks,links,lynx'
GRACEFUL_PAGER='less,more'
GRACEFUL_MEDIA_PLAYER='mpv,mplayer,vlc,ffplay'
GRACEFUL_IMAGE_VIEWER='meh,feh'

if "$PROFILE_LINUX"; then
    # Generate custom directory colors
    eval "$(dircolors "$DOTFILES/src/shell/dircolors")"

    # Cache generated colors
    cache LS_COLORS "$LS_COLORS"
fi
