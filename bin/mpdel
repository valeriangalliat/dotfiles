#!/bin/sh -e
#
# Delete the currently playing MPD song and play the next song.
#

current=$(mpc current -f '%file%')
dir=$(dirname "$current")
base=$(basename "$current")

rm -v "$HOME/music/$current"
echo "$base" >> "$HOME/music/$dir/removed.log"

mpc next
