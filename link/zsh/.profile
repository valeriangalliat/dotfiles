#
# ~/.profile
#
# Profile environment variables.
#
# See `~/.zshenv` to know more about `~/.cache/profile`.
#

# Include profile cache
[ -f ~/.cache/profile ] && . ~/.cache/profile

# Include flat profile
[ -f ~/.config/profile] && . ~/.config/profile
